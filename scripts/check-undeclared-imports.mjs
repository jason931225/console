// H-4 gate: every bare import specifier must be declared in the nearest package.json.
// STUB — the RED-test phase owns the contract; the implementation phase replaces this body.

/**
 * @param {string} _root repository root to scan
 * @returns {{ scanned: number, findings: { file: string, line: number, specifier: string }[] }}
 */
export function evaluateUndeclaredImports(_root) {
  throw new Error("unimplemented: evaluateUndeclaredImports");
}
